package Arrays;

/*
Array is indexed collection of fixed number of similar type of datatypes.

In array we can represent multiple values under the same name so the readablity of
the improved

Limitation of the array is once an array is created there is no way to increse or decrese 
its size 
 */
//package Day3;

/**
 *
 * @author boss 123
 */
public class Arrays {
    public static void main(String[] args) {
	int a1[],a2[],a3[];
        int[] array1;
        int array2[];
        int []array3;
        int[]arry4;
// At the time of declaration we can not specify size of array
//      int[5] aray5;


//valid two dimension array declaration
        int [][] array2d1;
        int [] array2d2[],j;
        int array2d3[][],i=10;
        System.out.println(i);

// when multiple arrays has to be declare
        int [][] array2d4,array2d5;//both are 2d
        int [] aryar2d6[],array2d7;//first is 2d second is 1d array
        int [][][][][] array2d8[],array2d9[];
//      int[] array2d10,[]array2d11; 
//only first array can be have dimension specified before itself 
        
        
    }
    
}
